on:
  pull_request:
    branches: main
    paths:
      - 'README.md'
  workflow_dispatch:

jobs:
  pr_check:
    name: render_book
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required" '
